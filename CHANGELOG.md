# Changelog

## v2.1.0 - 2026-08-24

- 在 v2 本地向导中新增按购方整理原件、生成购方分表与金额汇总的可选流程。
- 新增 `buyer_organizer.py`：购方索引、统计口径、安全唯一工作表名和购方维度汇总。
- 重复判断改为保守规则：仅 SHA256 完全相同文件的后续副本不计入建议合计；空号码和号码重复但内容不同全部保留并提示人工确认。
- 人工购方映射外置为私有 JSON；公共仓库只提供虚构示例，禁止写入真实公司和文件名。
- 新增购方处理测试，完整测试数增至 15 项。

## v2.0.0 - 2026-08-24

### 普通用户体验

- 新增仅监听 `127.0.0.1` 的本地网页配置向导。
- 新增 Windows 双击启动入口 `start-windows.bat` 与跨平台 `start.py`。
- 内置 QQ、163、Gmail、Outlook / Microsoft 365 邮箱预设。
- 新增测试连接、预览候选、确认后正式执行和安全退出。
- 新增邮箱模式与本地文件夹模式，兼容“从邮箱下载”和“整月 PDF 已放入文件夹”两种真实场景。

### 安全

- 保持 IMAP 只读连接。
- 本地向导不使用第三方前端资源，不保存密码。
- 增加 loopback Host 校验、随机会话令牌、请求大小限制和安全响应头。
- `.gitignore` 默认排除真实配置、附件、Excel 和运行摘要。

### 处理与交付

- 核心执行逻辑抽为可复用函数，保留原 CLI。
- `run-summary.json` 增加候选数、PDF 数、解析成功数、待人工数和重复号码数。
- 新增本地文件夹批量处理入口。
- 新增 GitHub README、配置说明、内容口径核对和自动化测试。

### 已知限制

- 自动解析仍主要面向带文本层的增值税电子发票 PDF。
- 扫描件、OFD、XML、图片、压缩包、加密文件与特殊版式仍需人工处理。
- Gmail 与 Microsoft 365 等邮箱可能更推荐 OAuth；当前开源版仍使用用户自行生成的应用专用密码或邮箱允许的 IMAP 凭证。
